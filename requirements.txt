# ATLAS — Track A (data layer) dependencies.
# Install into a fresh venv:  python3 -m venv .venv && ./.venv/bin/pip install -r requirements.txt
psycopg2-binary==2.9.12   # CockroachDB / Postgres driver
python-dotenv==1.2.2      # load .env (COCKROACH_DATABASE_URL, GEMINI_API_KEY)
google-genai==2.17.0      # Gemini embeddings (stand-in for Bedrock Titan while AWS verifies)
